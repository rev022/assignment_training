SELECT ENAME, JOB /* 1*/
FROM EMP
WHERE JOB LIKE 'CLERK';

/* 2*/
SELECT ENAME, JOB
FROM EMP
WHERE JOB LIKE 'CLERK' OR JOB LIKE 'MANAGER';

/* 3*/
SELECT CAST(GETDATE() AS DATE);

SELECT GETDATE();

/* 4*/
SELECT ENAME, SAL
FROM EMP
WHERE SAL BETWEEN 500 AND 1000;

/* 5*/
SELECT ENAME, HIREDATE
FROM EMP
WHERE MONTH(HIREDATE)='12';

/* 6*/
SELECT ENAME, SAL
FROM EMP
WHERE SAL NOT BETWEEN 1000 AND 2500;

/* 7*/
SELECT ENAME, SAL
FROM EMP
WHERE SAL IN (800, 950, 3000,5000);

/* 8*/
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '_L%';

/* 9*/
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%D';

/* 10*/
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '_____';

/* 11*/
SELECT ENAME
FROM EMP
WHERE ENAME LIKE 'T%R';

/* 12*/
SELECT ENAME
FROM EMP
WHERE ENAME LIKE '%A%';


/* 13*/
SELECT ENAME, HIREDATE,
DATEPART(DD, HIREDATE) AS 'DATE JOINED'
FROM EMP
WHERE DATEPART(DD, HIREDATE)<19
ORDER BY HIREDATE;


/* 14*/
SELECT ENAME, EMPNO
FROM EMP
WHERE ENAME = 'JAMES';


/* 15*/
SELECT ENAME, SAL
FROM EMP 
WHERE ENAME='KING';

/* 16*/
SELECT ENAME, JOB
FROM EMP 
WHERE JOB='PRESIDENT';

/* 17*/
SELECT *
FROM EMP
WHERE JOB='MANAGER';

/* 18*/
SELECT *
FROM EMP
WHERE COMM IS NOT NULL;



/* 19*/
SELECT ENAME, HIREDATE
FROM EMP
WHERE HIREDATE > '01-APR-82';


/* 20*/
SELECT ENAME, SAL
FROM EMP 
WHERE ENAME IN ('KING','Blake','Ford','Smith');


/* 21,22*/
SELECT ENAME, SAL
FROM EMP
WHERE SAL<3500;


/* 23*/
SELECT *
FROM EMP
WHERE JOB='CLERK';

/* 24*/
SELECT ENAME, SAL*12 AS 'ANNUAL SAL'
FROM EMP

/* 25*/
SELECT ENAME, (0.2*SAL)*12 AS 'ANNUAL SAL'
FROM EMP
WHERE ENAME='FORD';

/* 26*/
SELECT E.DEPTNO, D.DNAME, SUM(SAL)
FROM EMP E
INNER JOIN DEPT D ON E.DEPTNO=D.DEPTNO
GROUP BY E.DEPTNO, D.DNAME


/* 27*/
SELECT ENAME, SAL
FROM EMP
WHERE SAL =
	(SELECT MIN(SAL)
	FROM EMP
	WHERE DEPTNO=10)

	
	/* 28*/
	SELECT  AVG(SAL)AS 'AVG SAL'
	FROM EMP
	WHERE JOB = 'MANAGER';

	
/* 29*/

	SELECT  SUM(SAL)AS 'TOTAL SAL'
	FROM EMP
	WHERE JOB = 'SALESMAN';


/* 30*/
SELECT DEPTNO, AVG(SAL) AS 'AVG SAL'
FROM EMP
GROUP BY DEPTNO
HAVING AVG(SAL)< (SELECT AVG(SAL) FROM EMP)

/* 31*/
SELECT DEPTNO, COUNT(*)
FROM EMP
GROUP BY DEPTNO


/* 32*/
SELECT MAX(SAL) AS 'HIGHEST'
FROM EMP E
WHERE JOB = 'ANALYST';

 /* 32*/
SELECT ENAME, SAL, DEPTNO
FROM EMP
WHERE SAL IN 
(SELECT MAX(SAL) AS 'HIGHEST'
FROM EMP
WHERE JOB = 'ANALYST');

/* 33*/
SELECT MAX(COMM) AS 'HIGHEST'
FROM EMP

 /* 34*/
 SELECT COUNT(*)
 FROM DEPT

 /* 35*/
 SELECT ENAME 
 FROM EMP
 WHERE ENAME LIKE '___L%';
 

	
























